# this file is evaluated by .ci/build.sh

POM_CURRENT_VERSION="7.12.1-SNAPSHOT" # perform release if pom.xml matches this version
POM_RELEASE_VERSION="7.13.0" # next release version

DRY_RUN=false # is dry run?
SKIP_TESTS=true # skip tests during release build?
